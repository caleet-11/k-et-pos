<Window x:Class="POS_Presentacion.InfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Acerca del Sistema" Height="420" Width="400" 
        WindowStartupLocation="CenterScreen" 
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Border Background="White" CornerRadius="15" Margin="15">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Direction="-90" RenderingBias="Quality" ShadowDepth="2" Opacity="0.2"/>
        </Border.Effect>

        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Button Grid.Row="0" HorizontalAlignment="Right" VerticalAlignment="Top" 
                    Style="{StaticResource MaterialDesignToolButton}" 
                    Click="BtnCerrar_Click" Width="30" Height="30" Padding="0">
                <materialDesign:PackIcon Kind="Close" Width="24" Height="24" Foreground="Gray"/>
            </Button>

            <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                <materialDesign:PackIcon Kind="Storefront" Width="80" Height="80" Foreground="#2E7D32" HorizontalAlignment="Center" Margin="0,0,0,15"/>

                <TextBlock Text="Punto de Venta" FontSize="26" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#333333"/>
                <TextBlock Text="Versión 1.0.0 (Build 2026)" FontSize="15" Foreground="Gray" HorizontalAlignment="Center" Margin="0,5,0,25"/>

                <Separator Width="250" Margin="0,0,0,25" Background="#E0E0E0"/>

                <TextBlock Text="Desarrollado y Diseñado por:" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="#555"/>
                <TextBlock Text="Alejandro Espinoza Tafolla" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#1976D2" Margin="0,8,0,5"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5,0,0">
                    <materialDesign:PackIcon Kind="Email" Width="16" Height="16" Foreground="Gray" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBlock Text="contacto@tucorreo.com" FontSize="14" HorizontalAlignment="Center" Foreground="Gray" VerticalAlignment="Center"/>
                </StackPanel>
            </StackPanel>

            <TextBlock Grid.Row="2" Text="© 2026 Todos los derechos reservados." FontSize="12" Foreground="DarkGray" HorizontalAlignment="Center" Margin="0,15,0,0"/>
        </Grid>
    </Border>
</Window>