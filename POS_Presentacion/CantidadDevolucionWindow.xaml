<Window x:Class="POS_Presentacion.CantidadDevolucionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Unidades a Devolver" Height="240" Width="350" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" WindowStyle="ToolWindow"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="15"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid Margin="20">
        <StackPanel VerticalAlignment="Center">
            <materialDesign:PackIcon Kind="KeyboardReturn" Width="36" Height="36" Foreground="{DynamicResource PrimaryHueMidBrush}" HorizontalAlignment="Center" Margin="0,0,0,10"/>

            <TextBlock x:Name="lblInstruccion" Text="¿Cuántas unidades deseas devolver?" FontSize="16" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,0,0,15" HorizontalAlignment="Center"/>

            <TextBox x:Name="txtCantidad" FontSize="24" Height="40" HorizontalContentAlignment="Center" FontWeight="Bold" Margin="0,0,0,20" 
                     KeyDown="TxtCantidad_KeyDown" materialDesign:HintAssist.Hint="0" materialDesign:HintAssist.IsFloating="False"/>

            <Button Content="CONFIRMAR" Height="40" FontSize="15" Click="BtnConfirmar_Click" materialDesign:ButtonAssist.CornerRadius="5"/>
        </StackPanel>
    </Grid>
</Window>